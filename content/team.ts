// team.ts
// Nessun nome, ruolo o dato del team è stato fornito nel documento
// originale: la sezione resta vuota e pronta per essere popolata.

export type TeamMember = {
  name: string;
  role: string;
  photo?: string;
};

export const team: TeamMember[] = [];

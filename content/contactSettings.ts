// contactSettings.ts
// Riesporta i dati di contatto da siteSettings per compatibilità con la
// struttura di file richiesta nel brief (contactSettings.ts dedicato).

export { siteSettings } from "./siteSettings";
